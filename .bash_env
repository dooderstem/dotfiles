#!/bin/bash

# These are not to be pushed to any public repositories. These are for local testing only and should never be shared with anyone, as they contain sensitive information such as API keys.

#! Sourced in C:\Users\User\.bashrc

export OPENAI_API_KEY=""
