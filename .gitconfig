[filter "lfs"]
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
	clean = git-lfs clean -- %f
[user]
	name = DOODER(Michael Smith)
	email = skyviewsmith@gmail.com
[core]
	editor = micro
